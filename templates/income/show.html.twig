{% extends 'base.html.twig' %}

{% block title %}{{ income.description }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1 class="h2 mb-0">
                        <i class="bi bi-arrow-up-circle text-success me-2"></i>
                        Détail du revenu
                    </h1>
                    <p class="text-muted">{{ income.description }}</p>
                </div>
                <div>
                    <a href="{{ path('app_period_show', {id: income.period.id}) }}" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left"></i> Retour à la période
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-info-circle"></i>
                        Informations détaillées
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-8">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th class="text-muted" style="width: 40%;">Description :</th>
                                        <td class="fw-bold">{{ income.description }}</td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">Montant :</th>
                                        <td>
                                            <span class="h4 text-success mb-0">
                                                <i class="bi bi-currency-euro"></i>
                                                {{ income.amount|number_format(2, ',', ' ') }}
                                            </span>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">Date :</th>
                                        <td>
                                            <i class="bi bi-calendar3"></i>
                                            {{ income.date|date('d/m/Y') }}
                                            <small class="text-muted">
                                                ({{ income.date|date('l', 'Europe/Paris')|trans }})
                                            </small>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">Catégorie :</th>
                                        <td>
                                            <span class="badge bg-success bg-opacity-75 fs-6">
                                                <i class="bi bi-tag"></i>
                                                {{ income.category.name }}
                                            </span>
                                        </td>
                                    </tr>
                                    <tr>
                                        <th class="text-muted">Période :</th>
                                        <td>
                                            <a href="{{ path('app_period_show', {id: income.period.id}) }}" class="text-decoration-none">
                                                <i class="bi bi-calendar-event"></i>
                                                {{ income.period.name }}
                                            </a>
                                            <br>
                                            <small class="text-muted">
                                                {{ income.period.startDate|date('d/m/Y') }} - {{ income.period.endDate|date('d/m/Y') }}
                                            </small>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <div class="col-md-4 text-center">
                            <div class="bg-success bg-opacity-10 rounded p-3">
                                <i class="bi bi-arrow-up-circle text-success" style="font-size: 3rem;"></i>
                                <p class="text-success fw-bold mb-0 mt-2">REVENU</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h6 class="mb-0">
                        <i class="bi bi-gear"></i>
                        Actions
                    </h6>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a href="{{ path('app_income_edit', {id: income.id}) }}" class="btn btn-outline-primary">
                            <i class="bi bi-pencil-square"></i> Modifier
                        </a>
                        
                        <form method="post" action="{{ path('app_income_delete', {'id': income.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce revenu ?');">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ income.id) }}">
                            <button class="btn btn-outline-danger w-100">
                                <i class="bi bi-trash"></i> Supprimer
                            </button>
                        </form>
                        
                        <hr>
                        
                        <a href="{{ path('app_income_index') }}" class="btn btn-outline-secondary">
                            <i class="bi bi-list"></i> Tous les revenus
                        </a>
                        
                        <a href="{{ path('app_income_new', {period: income.period.id}) }}" class="btn btn-outline-success">
                            <i class="bi bi-plus-circle"></i> Nouveau revenu
                        </a>
                    </div>
                </div>
            </div>

            <div class="card shadow-sm mt-3">
                <div class="card-header bg-light">
                    <h6 class="mb-0">
                        <i class="bi bi-bar-chart"></i>
                        Statistiques de la période
                    </h6>
                </div>
                <div class="card-body">
                    {% set totalIncomes = 0 %}
                    {% set totalExpenses = 0 %}
                    {% for otherIncome in income.period.incomes %}
                        {% set totalIncomes = totalIncomes + otherIncome.amount %}
                    {% endfor %}
                    {% for expense in income.period.expenses %}
                        {% set totalExpenses = totalExpenses + expense.amount %}
                    {% endfor %}

                    <div class="mb-3">
                        <small class="text-muted d-block">Total revenus :</small>
                        <span class="text-success fw-bold">{{ totalIncomes|number_format(2, ',', ' ') }} €</span>
                    </div>
                    
                    <div class="mb-3">
                        <small class="text-muted d-block">Total dépenses :</small>
                        <span class="text-danger fw-bold">{{ totalExpenses|number_format(2, ',', ' ') }} €</span>
                    </div>
                    
                    <hr>
                    
                    <div>
                        <small class="text-muted d-block">Solde :</small>
                        {% set balance = totalIncomes - totalExpenses %}
                        <span class="fw-bold {{ balance >= 0 ? 'text-success' : 'text-danger' }}">
                            {{ balance|number_format(2, ',', ' ') }} €
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %} 