<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Dépenses - Budget Manager</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --success-gradient: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            --warning-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --info-gradient: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
            --bg-color: #f8f9fc;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--bg-color);
            color: #2c3e50;
            margin: 0;
            padding: 0;
        }

        .modern-nav {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(0,0,0,0.05);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 20px rgba(0,0,0,0.1);
        }

        .nav-brand {
            display: flex;
            align-items: center;
            font-weight: 700;
            font-size: 1.5rem;
            color: #2c3e50;
            text-decoration: none;
        }

        .nav-brand:hover {
            color: #667eea;
        }

        .brand-icon {
            width: 40px;
            height: 40px;
            background: var(--primary-gradient);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 12px;
            color: white;
            font-size: 1.2rem;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-link {
            color: #6c757d;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            color: #667eea;
            background: rgba(102, 126, 234, 0.1);
        }

        .nav-link.active {
            color: #667eea;
            background: rgba(102, 126, 234, 0.1);
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background: var(--primary-gradient);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
        }

        .page-container {
            padding: 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .page-header {
            background: var(--warning-gradient);
            border-radius: 24px;
            padding: 3rem;
            color: white;
            margin-bottom: 3rem;
            position: relative;
            overflow: hidden;
        }

        .page-header::before {
            content: '';
            position: absolute;
            top: -20%;
            right: -15%;
            width: 120px;
            height: 120px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
            animation: morph-bg 6s ease-in-out infinite;
        }

        .page-header::after {
            content: '';
            position: absolute;
            bottom: -25%;
            left: -8%;
            width: 80px;
            height: 80px;
            background: rgba(255, 255, 255, 0.08);
            border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
            animation: morph-bg-reverse 8s ease-in-out infinite;
        }

        @keyframes morph-bg {
            0%, 100% { 
                border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
                transform: translate(0, 0) rotate(0deg);
            }
            25% { 
                border-radius: 58% 42% 75% 25% / 76% 46% 54% 24%;
                transform: translate(-10px, -10px) rotate(45deg);
            }
            50% { 
                border-radius: 50% 50% 33% 67% / 55% 27% 73% 45%;
                transform: translate(-20px, 10px) rotate(90deg);
            }
            75% { 
                border-radius: 33% 67% 58% 42% / 63% 68% 32% 37%;
                transform: translate(10px, -15px) rotate(135deg);
            }
        }

        @keyframes morph-bg-reverse {
            0%, 100% { 
                border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
                transform: translate(0, 0) rotate(0deg);
            }
            33% { 
                border-radius: 70% 30% 46% 54% / 30% 29% 71% 70%;
                transform: translate(15px, 10px) rotate(-60deg);
            }
            66% { 
                border-radius: 100% 0% 0% 100% / 0% 100% 0% 100%;
                transform: translate(-10px, 15px) rotate(-120deg);
            }
        }

        .page-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            position: relative;
            z-index: 10;
        }

        .page-subtitle {
            font-size: 1.2rem;
            opacity: 0.9;
            margin-bottom: 2rem;
            position: relative;
            z-index: 10;
        }

        .header-actions {
            position: relative;
            z-index: 10;
        }

        .btn-modern {
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            text-decoration: none;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-modern:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            color: white;
        }

        .content-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
            border: 1px solid rgba(0,0,0,0.02);
            overflow: hidden;
        }

        .table-modern {
            margin: 0;
        }

        .table-modern thead {
            background: rgba(240, 147, 251, 0.05);
        }

        .table-modern thead th {
            border: none;
            padding: 1.5rem 1rem;
            font-weight: 600;
            color: #2c3e50;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .table-modern tbody tr {
            border-top: 1px solid rgba(0,0,0,0.05);
            transition: all 0.3s ease;
        }

        .table-modern tbody tr:hover {
            background: rgba(240, 147, 251, 0.02);
            transform: translateX(5px);
        }

        .table-modern tbody td {
            padding: 1.5rem 1rem;
            border: none;
            vertical-align: middle;
        }

        .expense-description {
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 0.25rem;
        }

        .period-link {
            color: #667eea;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .period-link:hover {
            color: #f093fb;
        }

        .period-info {
            font-size: 0.85rem;
            color: #6c757d;
        }

        .category-badge {
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            background: rgba(240, 147, 251, 0.1);
            color: #f093fb;
        }

        .amount-display {
            font-size: 1.2rem;
            font-weight: 700;
            color: #dc3545;
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }

        .btn-action {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            border: none;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .btn-view {
            background: rgba(102, 126, 234, 0.1);
            color: #667eea;
        }

        .btn-edit {
            background: rgba(108, 117, 125, 0.1);
            color: #6c757d;
        }

        .btn-delete {
            background: rgba(220, 53, 69, 0.1);
            color: #dc3545;
        }

        .btn-action:hover {
            transform: translateY(-2px);
            filter: brightness(1.1);
        }

        .table-footer {
            background: rgba(220, 53, 69, 0.05);
            padding: 1.5rem;
            border-top: 2px solid rgba(220, 53, 69, 0.1);
        }

        .total-amount {
            font-size: 1.4rem;
            font-weight: 700;
            color: #dc3545;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
        }

        .empty-icon {
            font-size: 4rem;
            color: #e9ecef;
            margin-bottom: 1.5rem;
        }

        .empty-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: #6c757d;
            margin-bottom: 1rem;
        }

        .empty-description {
            color: #adb5bd;
            margin-bottom: 2rem;
        }

        .btn-primary-modern {
            background: var(--warning-gradient);
            border: none;
            color: white;
            padding: 1rem 2rem;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
        }

        .btn-primary-modern:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(240, 147, 251, 0.3);
            color: white;
        }

        .modal-content {
            border-radius: 20px;
            border: none;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .modal-header {
            border-bottom: 1px solid rgba(0,0,0,0.05);
            padding: 1.5rem;
        }

        @media (max-width: 768px) {
            .page-container {
                padding: 1rem;
            }

            .page-header {
                padding: 2rem;
            }

            .page-title {
                font-size: 2rem;
            }

            .nav-links {
                display: none;
            }

            .table-responsive {
                border-radius: 20px;
            }

            .table-modern tbody tr:hover {
                transform: none;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation moderne -->
    <nav class="modern-nav">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <a href="{{ path('app_dashboard') }}" class="nav-brand">
                    <div class="brand-icon">
                        <i class="bi bi-wallet2"></i>
                    </div>
                    Budget Manager
                </a>

                <div class="nav-links">
                    <a href="{{ path('app_dashboard') }}" class="nav-link">Dashboard</a>
                    <a href="{{ path('app_period_index') }}" class="nav-link">Périodes</a>
                    <a href="{{ path('app_income_index') }}" class="nav-link">Revenus</a>
                    <a href="{{ path('app_expense_index') }}" class="nav-link active">Dépenses</a>
                    <a href="{{ path('app_category_index') }}" class="nav-link">Catégories</a>
                </div>

                <div class="user-avatar" title="{{ app.user.email }}">
                    {{ app.user.email|first|upper }}
                </div>
            </div>
        </div>
    </nav>

    <!-- Container principal -->
    <div class="page-container">
        <!-- En-tête de la page -->
        <div class="page-header">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h1 class="page-title">
                        <i class="bi bi-arrow-down-circle me-3"></i>Mes Dépenses
                    </h1>
                    <p class="page-subtitle">Contrôlez et analysez toutes vos dépenses</p>
                </div>
                <div class="header-actions">
                    <a href="{{ path('app_expense_new') }}" class="btn-modern">
                        <i class="bi bi-plus-circle"></i>
                        Nouvelle dépense
                    </a>
                </div>
            </div>
        </div>

        <!-- Contenu principal -->
        {% if expenses|length > 0 %}
            <div class="content-card">
                <div class="table-responsive">
                    <table class="table table-modern">
                        <thead>
                            <tr>
                                <th>Description</th>
                                <th>Période</th>
                                <th>Catégorie</th>
                                <th>Date</th>
                                <th class="text-end">Montant</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for expense in expenses %}
                            <tr>
                                <td>
                                    <div class="expense-description">{{ expense.description }}</div>
                                </td>
                                <td>
                                    <a href="{{ path('app_period_show', {id: expense.period.id}) }}" class="period-link">
                                        {{ expense.period.name }}
                                    </a>
                                    <div class="period-info">
                                        {{ expense.period.startDate|date('d/m/Y') }} - {{ expense.period.endDate|date('d/m/Y') }}
                                    </div>
                                </td>
                                <td>
                                    {% if expense.category %}
                                        <span class="category-badge">{{ expense.category.name }}</span>
                                    {% else %}
                                        <span class="text-muted">Non catégorisé</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <div>{{ expense.date|date('d/m/Y') }}</div>
                                    <small class="text-muted">{{ expense.date|date('l') }}</small>
                                </td>
                                <td class="text-end">
                                    <span class="amount-display">
                                        -{{ expense.amount|number_format(2, ',', ' ') }}€
                                    </span>
                                </td>
                                <td class="text-center">
                                    <div class="action-buttons">
                                        <a href="{{ path('app_expense_show', {id: expense.id}) }}" class="btn-action btn-view">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a href="{{ path('app_expense_edit', {id: expense.id}) }}" class="btn-action btn-edit">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <button type="button" class="btn-action btn-delete" 
                                                data-bs-toggle="modal" data-bs-target="#deleteModal{{ expense.id }}">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                
                <div class="table-footer">
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="fw-bold">Total des dépenses</span>
                        <span class="total-amount">
                            {% set totalExpense = 0 %}
                            {% for expense in expenses %}
                                {% set totalExpense = totalExpense + expense.amount %}
                            {% endfor %}
                            -{{ totalExpense|number_format(2, ',', ' ') }}€
                        </span>
                    </div>
                </div>
            </div>

            {# Modals de suppression #}
            {% for expense in expenses %}
            <div class="modal fade" id="deleteModal{{ expense.id }}" tabindex="-1">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title">Supprimer la dépense</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <div class="modal-body">
                            <p>Êtes-vous sûr de vouloir supprimer la dépense :</p>
                            <div class="alert alert-info">
                                <strong>{{ expense.description }}</strong><br>
                                Montant : {{ expense.amount|number_format(2, ',', ' ') }}€<br>
                                Date : {{ expense.date|date('d/m/Y') }}
                            </div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                            <form method="post" action="{{ path('app_expense_delete', {id: expense.id}) }}" style="display: inline;">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expense.id) }}">
                                <button class="btn btn-danger">
                                    <i class="bi bi-trash"></i> Supprimer
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}

        {% else %}
            <div class="empty-state">
                <div class="empty-icon">
                    <i class="bi bi-arrow-down-circle"></i>
                </div>
                <h3 class="empty-title">Aucune dépense enregistrée</h3>
                <p class="empty-description">Commencez par ajouter vos premières dépenses pour suivre votre budget.</p>
                <a href="{{ path('app_expense_new') }}" class="btn-primary-modern">
                    <i class="bi bi-plus-circle"></i>
                    Ajouter ma première dépense
                </a>
            </div>
        {% endif %}
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 